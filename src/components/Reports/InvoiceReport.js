import React from "react";
import { observer } from "mobx-react";


const InvoiceReport = () => {
  return (
    <div>
        InvoiceReport
    </div>
  );
};

export default observer(InvoiceReport);
