import React from "react";
import { observer } from "mobx-react";


const BookingReport = () => {
  return (
    <div>
        Booking Report
    </div>
  );
};

export default observer(BookingReport);
